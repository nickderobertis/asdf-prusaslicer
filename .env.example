# Copy to .env (untracked) and fill in if you hit GitHub API rate limits while
# developing. A token only raises the unauthenticated 60 requests/hour limit;
# it is never required for normal use. Do not commit a real token.
# GITHUB_API_TOKEN=
