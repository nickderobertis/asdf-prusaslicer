# Optional contributor convenience, loaded by direnv (https://direnv.net).
# Running `direnv allow` here is never required to *use* the plugin; it only
# loads developer environment variables from an untracked .env file (e.g. a
# GITHUB_API_TOKEN for higher API rate limits). Never put secrets in this file.
dotenv_if_exists
